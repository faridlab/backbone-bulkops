openapi: '3.0.3'
info:
  title: Backbone API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    This API provides 11 standard CRUD endpoints for each model.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: BulkJobs
    description: BulkJob management endpoints
  - name: BulkJobItems
    description: BulkJobItem management endpoints

paths:
  /api/v1/bulk_jobs:
    get:
      tags:
        - BulkJobs
      summary: List BulkJobs
      description: Get a paginated list of BulkJobs with optional filtering and sorting
      operationId: listBulkJobs
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - BulkJobs
      summary: Create BulkJob
      description: Create a new BulkJob
      operationId: createBulkJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBulkJobRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/bulk_jobs/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - BulkJobs
      summary: Get BulkJob by ID
      operationId: getBulkJob
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - BulkJobs
      summary: Update BulkJob (full)
      operationId: updateBulkJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBulkJobRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - BulkJobs
      summary: Update BulkJob (partial)
      operationId: patchBulkJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchBulkJobRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - BulkJobs
      summary: Delete BulkJob (soft)
      operationId: deleteBulkJob
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/bulk_jobs/bulk:
    post:
      tags:
        - BulkJobs
      summary: Bulk create BulkJobs
      operationId: bulkCreateBulkJobs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateBulkJobRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BulkJob'
    put:
      tags:
        - BulkJobs
      summary: Bulk update BulkJobs (full, atomic)
      operationId: bulkUpdateBulkJobs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateBulkJobRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - BulkJobs
      summary: Bulk partial-update BulkJobs (atomic)
      operationId: bulkPatchBulkJobs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_jobs/upsert:
    post:
      tags:
        - BulkJobs
      summary: Upsert BulkJob
      operationId: upsertBulkJob
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBulkJobRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'

  /api/v1/bulk_jobs/trash:
    get:
      tags:
        - BulkJobs
      summary: List deleted BulkJobs
      operationId: listDeletedBulkJobs
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobList'

  /api/v1/bulk_jobs/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - BulkJobs
      summary: Restore deleted BulkJob
      operationId: restoreBulkJob
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJob'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/bulk_jobs/empty:
    delete:
      tags:
        - BulkJobs
      summary: Empty trash for BulkJobs
      operationId: emptyTrashBulkJobs
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/bulk_jobs/delete/bulk:
    post:
      tags:
        - BulkJobs
      summary: Bulk soft-delete BulkJobs by ids (atomic)
      operationId: bulkDeleteBulkJobs
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_jobs/restore/bulk:
    post:
      tags:
        - BulkJobs
      summary: Bulk restore BulkJobs by ids (atomic)
      operationId: bulkRestoreBulkJobs
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_jobs/restore/all:
    post:
      tags:
        - BulkJobs
      summary: Restore all soft-deleted BulkJobs
      operationId: restoreAllBulkJobs
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/bulk_jobs/trash/bulk:
    delete:
      tags:
        - BulkJobs
      summary: Bulk permanently-delete BulkJobs from trash by ids (atomic)
      operationId: bulkPermanentDeleteBulkJobs
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_job_items:
    get:
      tags:
        - BulkJobItems
      summary: List BulkJobItems
      description: Get a paginated list of BulkJobItems with optional filtering and sorting
      operationId: listBulkJobItems
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItemList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - BulkJobItems
      summary: Create BulkJobItem
      description: Create a new BulkJobItem
      operationId: createBulkJobItem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBulkJobItemRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/bulk_job_items/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - BulkJobItems
      summary: Get BulkJobItem by ID
      operationId: getBulkJobItem
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - BulkJobItems
      summary: Update BulkJobItem (full)
      operationId: updateBulkJobItem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBulkJobItemRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - BulkJobItems
      summary: Update BulkJobItem (partial)
      operationId: patchBulkJobItem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchBulkJobItemRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - BulkJobItems
      summary: Delete BulkJobItem (soft)
      operationId: deleteBulkJobItem
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/bulk_job_items/bulk:
    post:
      tags:
        - BulkJobItems
      summary: Bulk create BulkJobItems
      operationId: bulkCreateBulkJobItems
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateBulkJobItemRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BulkJobItem'
    put:
      tags:
        - BulkJobItems
      summary: Bulk update BulkJobItems (full, atomic)
      operationId: bulkUpdateBulkJobItems
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                allOf:
                  - type: object
                    required: [id]
                    properties:
                      id:
                        type: string
                  - $ref: '#/components/schemas/UpdateBulkJobItemRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItemBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'
    patch:
      tags:
        - BulkJobItems
      summary: Bulk partial-update BulkJobItems (atomic)
      operationId: bulkPatchBulkJobItems
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - type: object
                  required: [ids, patch]
                  properties:
                    ids:
                      type: array
                      items: { type: string }
                    patch:
                      type: object
                      additionalProperties: true
                - type: object
                  required: [items]
                  properties:
                    items:
                      type: array
                      items:
                        type: object
                        required: [id, patch]
                        properties:
                          id: { type: string }
                          patch: { type: object, additionalProperties: true }
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItemBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_job_items/upsert:
    post:
      tags:
        - BulkJobItems
      summary: Upsert BulkJobItem
      operationId: upsertBulkJobItem
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateBulkJobItemRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'

  /api/v1/bulk_job_items/trash:
    get:
      tags:
        - BulkJobItems
      summary: List deleted BulkJobItems
      operationId: listDeletedBulkJobItems
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItemList'

  /api/v1/bulk_job_items/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - BulkJobItems
      summary: Restore deleted BulkJobItem
      operationId: restoreBulkJobItem
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItem'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/bulk_job_items/empty:
    delete:
      tags:
        - BulkJobItems
      summary: Empty trash for BulkJobItems
      operationId: emptyTrashBulkJobItems
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

  /api/v1/bulk_job_items/delete/bulk:
    post:
      tags:
        - BulkJobItems
      summary: Bulk soft-delete BulkJobItems by ids (atomic)
      operationId: bulkDeleteBulkJobItems
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Soft-deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  soft_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_job_items/restore/bulk:
    post:
      tags:
        - BulkJobItems
      summary: Bulk restore BulkJobItems by ids (atomic)
      operationId: bulkRestoreBulkJobItems
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BulkJobItemBulkResult'
        '400':
          $ref: '#/components/responses/BadRequest'

  /api/v1/bulk_job_items/restore/all:
    post:
      tags:
        - BulkJobItems
      summary: Restore all soft-deleted BulkJobItems
      operationId: restoreAllBulkJobItems
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                type: object
                properties:
                  restored: { type: integer }

  /api/v1/bulk_job_items/trash/bulk:
    delete:
      tags:
        - BulkJobItems
      summary: Bulk permanently-delete BulkJobItems from trash by ids (atomic)
      operationId: bulkPermanentDeleteBulkJobItems
      requestBody:
        $ref: '#/components/requestBodies/BatchIds'
      responses:
        '200':
          description: Permanently deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  permanently_deleted: { type: integer }
        '400':
          $ref: '#/components/responses/BadRequest'

components:
  schemas:
    BulkJob:
      type: object
      required:
        - id
        - company_id
        - operation_type
        - target_module
        - status
        - total_items
        - succeeded_count
        - failed_count
        - metadata
      properties:
        id:
          type: string
          format: uuid
        company_id:
          type: string
          format: uuid
        operation_type:
          type: string
        target_module:
          type: string
        status:
          type: string
        total_items:
          type: integer
          format: int32
        succeeded_count:
          type: integer
          format: int32
        failed_count:
          type: integer
          format: int32
        submitted_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    BulkJobBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BulkJob'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateBulkJobRequest:
      type: object
      required:
        - company_id
        - operation_type
        - target_module
        - status
        - total_items
        - succeeded_count
        - failed_count
        - metadata
      properties:
        company_id:
          type: string
          format: uuid
        operation_type:
          type: string
        target_module:
          type: string
        status:
          type: string
        total_items:
          type: integer
          format: int32
        succeeded_count:
          type: integer
          format: int32
        failed_count:
          type: integer
          format: int32
        submitted_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    UpdateBulkJobRequest:
      type: object
      required:
        - company_id
        - operation_type
        - target_module
        - status
        - total_items
        - succeeded_count
        - failed_count
        - metadata
      properties:
        company_id:
          type: string
          format: uuid
        operation_type:
          type: string
        target_module:
          type: string
        status:
          type: string
        total_items:
          type: integer
          format: int32
        succeeded_count:
          type: integer
          format: int32
        failed_count:
          type: integer
          format: int32
        submitted_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    PatchBulkJobRequest:
      type: object
      properties:
        company_id:
          type: string
          format: uuid
        operation_type:
          type: string
        target_module:
          type: string
        status:
          type: string
        total_items:
          type: integer
          format: int32
        succeeded_count:
          type: integer
          format: int32
        failed_count:
          type: integer
          format: int32
        submitted_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    BulkJobList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BulkJob'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    BulkJobItem:
      type: object
      required:
        - id
        - job_id
        - item_key
        - status
        - payload
        - metadata
      properties:
        id:
          type: string
          format: uuid
        job_id:
          type: string
          format: uuid
        item_key:
          type: string
        status:
          type: string
        payload:
          type: string
        applied_ref_type:
          nullable: true
          type: string
        applied_ref_id:
          nullable: true
          type: string
          format: uuid
        error_detail:
          nullable: true
          type: string
        metadata:
          type: object

    BulkJobItemBulkResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BulkJobItem'
        total: { type: integer }
        failed: { type: integer }
        errors:
          type: array
          items: { type: string }

    CreateBulkJobItemRequest:
      type: object
      required:
        - job_id
        - item_key
        - status
        - payload
        - metadata
      properties:
        job_id:
          type: string
          format: uuid
        item_key:
          type: string
        status:
          type: string
        payload:
          type: string
        applied_ref_type:
          nullable: true
          type: string
        applied_ref_id:
          nullable: true
          type: string
          format: uuid
        error_detail:
          nullable: true
          type: string
        metadata:
          type: object

    UpdateBulkJobItemRequest:
      type: object
      required:
        - job_id
        - item_key
        - status
        - payload
        - metadata
      properties:
        job_id:
          type: string
          format: uuid
        item_key:
          type: string
        status:
          type: string
        payload:
          type: string
        applied_ref_type:
          nullable: true
          type: string
        applied_ref_id:
          nullable: true
          type: string
          format: uuid
        error_detail:
          nullable: true
          type: string
        metadata:
          type: object

    PatchBulkJobItemRequest:
      type: object
      properties:
        job_id:
          type: string
          format: uuid
        item_key:
          type: string
        status:
          type: string
        payload:
          type: string
        applied_ref_type:
          nullable: true
          type: string
        applied_ref_id:
          nullable: true
          type: string
          format: uuid
        error_detail:
          nullable: true
          type: string
        metadata:
          type: object

    BulkJobItemList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BulkJobItem'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  requestBodies:
    BatchIds:
      required: true
      content:
        application/json:
          schema:
            type: object
            required: [ids]
            properties:
              ids:
                type: array
                items: { type: string }

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
