//! BulkJob seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial BulkJob data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BULKJOB SEEDER
// ============================================================================

/// Seeder for bulk_job
pub struct SeedBulkJobSeeder;

impl SeedBulkJobSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBulkJobSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBulkJobSeeder {
    fn name(&self) -> &'static str {
        "SeedBulkJobSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if bulkops.bulk_jobs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM bulkops.bulk_jobs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/bulk_job_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM bulkops.bulk_jobs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

